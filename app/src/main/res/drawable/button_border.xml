<!-- button_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/gradient_start_color" /> <!-- Adjust border width and color -->
    <corners android:radius="5dp" /> <!-- Adjust corner radius -->
</shape>